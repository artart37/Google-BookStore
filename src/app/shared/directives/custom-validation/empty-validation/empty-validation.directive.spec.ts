import { EmptyValidationDirective } from './empty-validation.directive';

describe('EmptyValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyValidationDirective();
    expect(directive).toBeTruthy();
  });
});
